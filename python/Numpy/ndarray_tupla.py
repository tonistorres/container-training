"""
Para criar um ndarray, podemos passar uma lista,
tupla ou qualquer objeto do tipo array para o array() método,
e ele será convertido em um ndarray:
"""

import numpy as np


arr = np.array((1, 2, 3, 4, 5))

print(arr)
print(type(arr))
