"""
Criar um objeto NumPy ndarray
NumPy é usado para trabalhar com arrays. O objeto array no NumPy
é chamado ndarray.

Podemos criar um ndarrayobjeto NumPy usando a array()função.
Nota: type(): Essa função interna do Python nos informa o tipo do
objeto passado para ela. Como no código acima, mostra que arré numpy.
ndarray tipo.

"""
import numpy as np

arr = np.array(["marina", "da", "silva"])

print(type(arr))
