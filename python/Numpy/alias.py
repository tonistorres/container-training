"""
NumPy como np
NumPy geralmente é importado sob o np alias.
alias: em Python, alias é um nome alternativo para se referir
à mesma coisa.
Crie um alias com a palavra- aschave durante a importação:
"""

import numpy as np

# Agora, o pacote NumPy pode ser referido como npem vez de numpy.

array_alias = np.array(["ana", "paula"])
print(array_alias)
